// ==============================================
//  Свинцовая руда только в диких землях (1500+)
// ==============================================

const LEAD_SAFE_DIST = 1500
const LEAD_ORE = 'cgs:lead_ore'

// Заменитель руды в зоне строительства (на что менять)
const REPLACE_WITH = 'minecraft:stone'

// Обрабатываем чанки при первой загрузке (после генерации)
LevelEvents.beforeExplosion(event => {}) // заглушка, если не нужно

// Основная логика — сканируем сгенерированные чанки в опасной близости
// и убираем свинец, если чанк ближе 1500 блоков
ServerEvents.loaded(event => {
  // событие вызывается один раз при загрузке сервера
})

// Используем событие генерации через BlockEvents недоступно напрямую,
// поэтому применяем tick-скан загруженных чанков ОДИН раз